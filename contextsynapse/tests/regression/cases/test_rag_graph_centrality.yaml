id: test_rag_005
name: RAG - Graph Centrality Ranking
description: 'Tests RAG query using graph centrality for ranking.

  Uses node centrality to prioritize context.


  Query flow:

  1. Find relevant chunks

  2. Calculate centrality scores

  3. Rank by centrality + relevance

  4. LLM generation with top-ranked context

  '
namespace: regression_test
expected_success: true
query: ''
tags:
- rag
- graph_centrality
- ranking
verify_persistence: true
test_both_modes: true
approved_by: system
notes: 'Tests graph centrality for RAG ranking.

  Uses centrality scores to prioritize context.

  Uses explicit embedding model.

  '
priority: 5
prerequisites:
- RUN_INGESTION_PIPELINE
requires_data: true
requires_files: []

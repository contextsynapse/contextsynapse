id: test_rag_004
name: RAG Query - Weighted Graph Search
description: 'Tests RAG query with weighted graph search:

  - Edge weights influence traversal

  - Weighted scoring for relevance

  - Combines with vector search


  Uses GPT-3.5-Turbo with custom prompt and embedding model.

  '
query: RAG "What are the strongest business relationships?" AT NAMESPACE regression_test
namespace: regression_test_graph
expected_success: true
tags:
- rag
- graph_search
- weighted
verify_persistence: false
test_both_modes: true
approved_by: system
notes: 'Tests RAG query for weighted relationship analysis.

  Note: Weight-based filtering should be configured via pipeline.

  '
priority: 5
prerequisites:
- RUN_INGESTION_PIPELINE
requires_data: true
requires_files: []
